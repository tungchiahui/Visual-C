//1.编程序将单词“China”加密成密文，加密规则是：用原字母后面第4个字母代替原字母。（用ASCII码操作）



//2.从键盘输入圆环的外内半径值，编程序计算圆环的面积。要求π的取值为3.1415926，输入与输出均采用双精度。

#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
#define pai 3.1415926
int main()
{
	double r,s;
	printf("%s", "请输入半径：");
		scanf("%lf", &r);
		s = r * r * pai;
		printf("面积是: %lf", s);
		return 0;
}


//3.交换两个变量的值，由终端输入两个整数给变量x、y，然后交换x和y的值后，输出x和y。




//4.输入一个整数x，如果x的值大于0请输出数值1，如果小于0请输出数值-1，如果等于0请输出数值0。

#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
int main()
{
	int x;
	printf("请输入x:");
	scanf("%d", &x);
	if (x > 0) {
		printf("%d",1);
	}
	else
	{
		if (x < 0) {
			printf("%d", -1);
		}
		else {
			printf("%d", 0);
		}
		return 0;
	}
}

//5.输入两个整数，按照由大到小的顺序排序，最后输出。（提升题：可尝试输入三个由大到小排序）




//6.输入1~7的数字，显示出其代表的礼拜，并打印输出。

#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
int main()
{
	int a;
	scanf("%d", &a);
	switch (a) {
	case 7:printf("星期日\n"); break;
	case 6:printf("星期六\n"); break;
	case 5:; printf("星期五\n"); break;
	case 4:printf("星期四\n"); break;
	case 3:printf("星期三\n"); break;
	case 2:printf("星期二\n"); break;
	case 1:printf("星期一\n"); break;
	}
	return 0;
}
